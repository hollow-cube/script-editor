# Loaded by Vite for production builds (`vite build` / `bun run build:web`).
# The editor is served from https://hollowcube.net but the API lives on a
# different origin — this MUST be absolute and cross-origin, never same-origin.
VITE_API_BASE_URL=https://api.hollowcube.net
