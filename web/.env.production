# Committed prod default. Loaded by `vite build` (mode=production) and inlined
# into the bundle. Editor and API are cross-origin in prod (editor on
# hollowcube.net, API on api.hollowcube.net).
VITE_API_BASE_URL=https://api.hollowcube.net
