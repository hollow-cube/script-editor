# Loaded by Vite in dev (`vite` / `bun run dev:web`).
# Dev hits Envoy directly at :10000 so the request origin == the DPoP `htu`
# the backend reconstructs (a same-origin Vite proxy would make htu :5173 and
# every proof would 401).
VITE_API_BASE_URL=http://localhost:10000
